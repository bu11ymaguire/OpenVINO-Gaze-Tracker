
# OpenVINO Gaze Estimation Project - Setup Status

## Phase 1: 완료
1.  **라이브러리 설치**: `openvino-dev`, `openvino`, `opencv-python`, `numpy` 설치 완료.
2.  **모델 다운로드**: `models/intel` 폴더에 모델들이 다운로드되었습니다.
3.  **프로젝트 구조 생성**: `src` 폴더 및 `main.py` 생성 완료.

## Phase 2: 다음 단계
1.  **모델 로더 테스트**
2.  **얼굴 감지(Face Detection) 연동**
3.  **나머지 모델 순차 연결**

바로 **모델 로더 구현과 얼굴 감지 테스트**로 넘어가시겠습니까?
